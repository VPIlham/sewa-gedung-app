<%- include('../../navbar_admin') %>

<div class="row justify-content-center mt-5">
  <div class="col-md-9">
    <table class="table table-striped table-hover" id="mytable">
      <thead>
        <tr>
          <th scope="col">No</th>
          <th scope="col">Kode Transaksi</th>
          <th scope="col">Nama Pemesan</th>
          <th scope="col">Nomor HP</th>
          <th scope="col">Harga</th>
          <th scope="col">Bukti Pembayaran</th>
          <th scope="col">Nama Gedung</th>
          <th scope="col">Tgl Sewa</th>
          <th scope="col">Status</th>
          <th scope="col">Aksi</th>
        </tr>
      </thead>
      <tbody>
        <% result.forEach((pesanan, i) => { %>
        <tr>
          <th scope="row"><%= i + 1 %></th>
          <td class="text-capitalize"><%= pesanan.kode_transaksi%></td>
          <td class="text-capitalize"><%= pesanan.nama_pemesan %></td>
          <td class="text-capitalize"><%= pesanan.nomor_hp %></td>
          <td>Rp<%= pesanan.total_harga %></td>
          <td class="text-capitalize">
            <img
              src="/<%= pesanan.img %>"
              alt=""
              style="width: 70px; height: 70px; object-fit: cover"
            />
          </td>
          <td class="text-uppercase"><%= pesanan.nama_gedung %></td>
          <td class="text-capitalize">
            <%= new Date(pesanan.sewaTgl).toLocaleDateString("id") %>, <%=
            pesanan.sewaJam %> : <%= pesanan.sewaWaktu %>
          </td>
          <td class="text-capitalize"><%= pesanan.status %></td>
          <td>
            <a
              href="/admin/pemesanan/edit/<%= pesanan.id %>"
              class="badge text-bg-primary text-decoration-none"
              >Edit</a
            >
            |
            <a
              href="/admin/pemesanan/hapus/<%= pesanan.id %>"
              class="badge text-bg-danger text-decoration-none"
              >Hapus</a
            >
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>
